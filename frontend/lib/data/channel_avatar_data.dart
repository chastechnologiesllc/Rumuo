/// Production stub — avatar URLs come from the `sources` table via the API.
/// The hard-coded map of 500+ YouTube channel avatar URLs has been removed.
class ChannelAvatarData {
  ChannelAvatarData._();
  static const Map<String, String> byChannelId = {};
}
